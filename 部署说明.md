# 木材智能切割优化引擎 - 部署说明

## 一、环境要求

- **Python**：3.10 及以上（后端 API）
- **Node.js**：18 及以上（前端构建与开发，需带 npm）

## 二、依赖安装

### 1. 后端（Python）

在项目根目录下执行：

```bash
# 建议使用虚拟环境（可选）
python -m venv .venv
source .venv/bin/activate   # Linux/macOS
# 或 Windows: .venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

主要依赖库：

| 库名 | 用途 |
|------|------|
| fastapi | Web 框架，提供订单与木材接口 |
| uvicorn | ASGI 服务器，运行 FastAPI |
| pydantic | 请求/响应与领域模型校验 |

### 2. 前端（Node.js）

```bash
cd frontend
npm install
```

主要依赖：React 18、Vite 5（开发与构建）。

## 三、启动方式

### 1. 启动后端

在**项目根目录**（与 `main.py` 同级）执行：

```bash
python main.py
```

- 默认监听：**http://0.0.0.0:8765**
- 支持热重载（改代码后自动重启）
- 接口文档：http://127.0.0.1:8765/docs

### 2. 启动前端

在**新终端**中执行：

```bash
cd frontend
npm run dev
```

- 默认地址：**http://localhost:5173**（以 Vite 输出为准）
- 前端会请求后端 `http://127.0.0.1:8765`，请确保后端已先启动

### 3. 使用流程

1. 先启动后端，再启动前端。
2. 浏览器打开前端地址（如 http://localhost:5173）。
3. 在「订单配置」中提交订单。
4. 在「木材切割」中选择木材、预览展开图，再点击「提交并计算切割方案」查看结果。

## 四、生产部署简要说明

- **后端**：可用 `uvicorn main:app --host 0.0.0.0 --port 8765`（去掉 `reload`）。如需多进程可加 `--workers N`。
- **前端**：先构建静态资源：
  ```bash
  cd frontend && npm run build
  ```
  再将 `frontend/dist` 用 Nginx 等托管，或由后端挂载静态目录；前端需能访问后端 API（若域名/端口不同，需改前端的 `API_BASE` 或通过反向代理统一）。

## 五、常见问题

- **端口 8765 已被占用**：结束占用进程（如 `lsof -i :8765` 后 `kill -9 <PID>`），或修改 `main.py` 末尾的 `port`。
- **前端请求失败**：确认后端已启动且地址为 http://127.0.0.1:8765；若部署在不同机器，需在 `frontend/src/App.jsx` 中修改 `API_BASE`。
